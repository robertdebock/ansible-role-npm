---
- name: Converge
  hosts: all
  gather_facts: false

  roles:
    - robertdebock.bootstrap
    - ansible-role-npm

  tasks:
    - name: install package with npm
      npm:
        name: debug
        global: true
